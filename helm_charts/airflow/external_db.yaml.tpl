externalDatabase:
  type: ${external_db_type}
  host: ${external_db_host}
  port: ${external_db_port}
  database: ${external_db_name}
  user: ${external_db_user}
  password: ${external_db_secret}
postgresql:
  enabled: false